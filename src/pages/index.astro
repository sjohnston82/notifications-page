---
import Notification from "../components/Notification.astro";
import NotificationsBar from "../components/NotificationsBar";
import NotificationsLayout from "../layouts/NotificationsLayout.astro";
import { $notifications } from '../store/notificationsStore'


const notifications = $notifications.value;

---

<NotificationsLayout>
	<main class="w-[100%] h-[100dvh] px-4">
		<NotificationsBar client:load />
		<div class="flex flex-col gap-6 pb-8">
			{notifications?.map((notification) => <Notification  name={notification.name} type={notification.type} time={notification.time} postName={notification.postName} commentedPicture={notification.commentedPicture} avatar={notification.avatar} unread={notification.unread} message={notification.message} />)}
		</div>
	</main>
</NotificationsLayout>
		



../store/notificationsStore